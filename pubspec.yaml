name: go_router_sample
description: go_router sample project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  simple_logger:
  flutter_hooks:
  hooks_riverpod:
  freezed_annotation:
  go_router: ^12.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic_mono:
  json_serializable:
  build_runner:
  freezed:
  go_router_builder: ^2.3.4

flutter:
  uses-material-design: true
